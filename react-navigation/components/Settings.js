
import {
  View,
  Text,
  ScrollView
} from "react-native";

function Settings({ navigation }) {

  return (
    <ScrollView>
      <View style={{ gap: 10, marginTop: 20 }}>
        <Text>Settings</Text>
      </View>
    </ScrollView>
  );
}
export default Settings;
